Generate a JSON configuration for ONLY these specific experts (use exact names as shown):

{expert_descriptions}

CRITICAL RULES:
1. You MUST use the EXACT expert names from the list above (the part before the colon)
2. If an expert is marked as "(interaction expert)", put it in "interaction_experts"
3. All other experts go in "single_particle_experts"
4. DO NOT create any new expert names - use ONLY the ones listed above

Consider:
- Which species are mentioned in each behavior description
- The relative importance/strength of each behavior (adjust weights)
- Whether behaviors should apply to all species or specific ones

{species_context}

Generate the JSON configuration using ONLY the expert names listed above: