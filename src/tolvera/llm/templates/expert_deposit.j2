{# Jinja2 template for generating deposit expert functions #}
{# This template is for writing to the environment (pixels, trails, etc.) #}
@ti.func
def expert_{{ name }}({{ parameters }}):
    """{{ description }}"""
    # TYPED HOLE[None]: Deposit logic
    # Available inputs: {{ available_inputs }}
    {% if available_states %}
    # Available states: {{ available_states }}
    {% endif %}
    # Type constraints:
    # - No return value (void function)
    # - Can write to tv.px.px.rgba[x, y]
    # - Can use tv.px drawing functions
    # - Ensure bounds checking for pixel access
    
    {% if state_declarations %}
    # Type-annotated state access
    {{ state_declarations | indent(4) }}
    {% endif %}
    
    # Deposit logic (generated by LLM)
    {{ deposit_logic | indent(4) }}