Generate a Taichi sensor expert function for the following behavior:

Description: {description}

{state_context}

IMPORTANT STATE ACCESS RULES:
- Only access states explicitly listed in the state context above
- DO NOT invent or assume states exist (like pheromone_sensing_range)
- If you need a constant value, define it locally in the function
- Common constants to define locally:
  - sensor_distance = 50.0  # How far to sense
  - sensor_angle = 45.0     # Sensing angle in degrees
  - grid_size = 100         # Grid dimensions
  - cell_size = 10.0        # Size of each grid cell

The function should:
1. Read environmental information
2. Process/aggregate the sensed data
3. Return a single value as ti.f32

Function signature:
@ti.func
def expert_{name}({parameters}) -> ti.f32:
    # Your implementation here
    
Make sure to:
- Only READ from the environment (no modifications)
- Return meaningful values (0.0 if nothing sensed)
- Use efficient algorithms for parallel execution
- Handle edge cases gracefully

Common sensor outputs:
- Counts: number of neighbors, obstacles, etc.
- Gradients: difference between samples
- Averages: mean of local properties
- Distances: to nearest object of interest
- Concentrations: density or intensity values

Focus on gathering information ONLY. Do not include decision-making or actions.