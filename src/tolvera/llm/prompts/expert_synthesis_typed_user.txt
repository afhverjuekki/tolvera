Generate a force calculation for: "{description}"

{state_context}

Example force calculations:

# Gravity (constant downward force):
force = ti.math.vec2(0.0, -9.8) * mass

# Attraction to center:
center = ti.math.vec2(0.5, 0.5)
to_center = center - pos
dist = to_center.norm()
if dist > 0.001:
    force = to_center.normalized() * 50.0

# Repulsion between particles:
diff = p1.pos - p2.pos
dist = diff.norm()
if dist > 0.001 and dist < 0.1:
    force = diff.normalized() * (100.0 / (dist * dist))

Now generate:
1. Function name: [descriptive name based on the behavior]
2. Force calculation code (assign to 'force' variable)