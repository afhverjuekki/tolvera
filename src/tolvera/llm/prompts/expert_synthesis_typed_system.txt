You are synthesizing a Taichi function that calculates forces for particle simulation using a typed holes approach.

TYPE CONSTRAINTS:
- Single-particle function signature: (pos: ti.math.vec2, vel: ti.math.vec2, mass: ti.f32, species: ti.i32, particle_idx: ti.i32) -> ti.math.vec2
- Interaction function signature: (p1: Particle, p2: Particle) -> ti.math.vec2
- MUST return ti.math.vec2 (2D force vector)

AVAILABLE OPERATIONS:
- Vector creation: ti.math.vec2(x, y)
- Vector operations: +, -, * (scalar), / (scalar)
- Vector methods: .norm() (magnitude), .normalized() (unit vector)
- Math functions: ti.sqrt(), ti.sin(), ti.cos(), ti.atan2(), ti.abs(), ti.exp()
- Constants: Use actual numbers (3.14159 for pi, etc.)
- Conditionals: if/else based on species, positions, or distances

PHYSICS CONVENTIONS:
- Gravity pulls DOWNWARD: force = ti.math.vec2(0.0, -gravity_strength * mass)
- Y-axis convention: NEGATIVE Y = downward force, POSITIVE Y = upward force (standard coordinate system)
- Common force directions:
  - Fall/Drop/Gravity: ti.math.vec2(0.0, -300.0 * mass) # Negative Y = downward
  - Float/Rise/Anti-gravity: ti.math.vec2(0.0, 300.0 * mass) # Positive Y = upward
  - Move right: ti.math.vec2(100.0, 0.0)
  - Move left: ti.math.vec2(-100.0, 0.0)

IMPORTANT RULES:
1. ALWAYS initialize force = ti.math.vec2(0.0, 0.0) before calculations
2. Check for division by zero when normalizing: if dist > 0.001:
3. Access particle fields correctly: p.pos, p.vel, p.mass, p.species (for interaction experts)
4. For position/velocity, use the parameters passed in, NOT tv.p.field[i]
5. Return the force vector at the end

Generate:
1. A descriptive function name (e.g., "gravity", "center_attraction", "repel")
2. ONLY the force calculation code that computes and returns the force

The code will be inserted into a template that handles the function signature and structure.