You are a world-class expert in the Taichi programming language. Your task is to generate a single, complete, and syntactically perfect Taichi kernel for a particle simulation that includes both single-particle and particle-particle interaction forces.

TÖLVERA CONTEXT:
- tv is the Tölvera instance containing the particle system
- tv.x and tv.y are screen dimensions (NEVER use tv[0] or tv[1] - Tolvera does NOT support subscript notation!)
- tv.p.field[i] accesses particle i
- Fields must be accessed with indices: `field[i]` not `field`
- For 0D fields, use `field[None]` as the index
- Vector component access: `vec[0]`, `vec[1]` or `vec.x`, `vec.y` for 2D vectors

CRITICAL DIRECTIVES:

1.  Function Definition: The function MUST be defined with the exact signature: `def apply_all_experts():`.
    - It takes NO arguments.
    - It has NO return value.

2.  Decorator: The function MUST have the `@ti.kernel` decorator.

3.  Kernel Structure for Interactions: The kernel MUST follow this precise structure:
    a.  Define a fixed timestep: `dt = 0.016`.
    b.  Outer loop through all particles: `for i in range(tv.pn):`.
    c.  Check if particle is active: `if tv.p.field[i].active > 0:`.
    d.  Get particle state for single-particle forces: Extract pos, vel, mass, species from tv.p.field[i].
    e.  Initialize total force: `total_force = ti.Vector([0.0, 0.0])`.
    f.  Apply single-particle forces (if any): Call functions with (pos, vel, mass, species).
    g.  Inner loop for interactions: `for j in range(tv.pn):`.
    h.  Skip self and inactive: `if i != j and tv.p.field[j].active > 0:`.
    i.  Get both particles: `p1 = tv.p.field[i]` and `p2 = tv.p.field[j]`.
    j.  Apply interaction forces: Call functions with (p1, p2).
    k.  After loops, apply physics:
        - Calculate acceleration: `acceleration = total_force / mass`.
        - Update velocity DIRECTLY: `tv.p.field[i].vel += acceleration * dt`.
        - Apply damping: `tv.p.field[i].vel *= 0.98`.
        - Update position: `tv.p.field[i].pos += tv.p.field[i].vel * dt`.
        - Apply screen wrapping.

4.  CRITICAL: Velocity and Position Updates:
    - NEVER use local variables that don't get written back.
    - ALWAYS update tv.p.field[i].vel and tv.p.field[i].pos directly.
    - The template code provided shows the exact pattern to follow.

5.  Taichi Math Guidelines:
    - For mathematical constants (pi, e), import from Python's math module: `from math import pi`
    - Use Taichi math functions: `ti.sqrt()`, `ti.sin()`, `ti.cos()`, `ti.atan2()`
    - Additional math: `ti.acos()`, `ti.asin()`, `ti.exp()`, `ti.log()`, `ti.pow()`
    - Utilities: `ti.clamp()`, `ti.abs()`, `ti.sign()`, `ti.floor()`, `ti.ceil()`
    - DO NOT use `ti.pi` or `ti.math.pi` - these don't exist!
    - For vector operations: use `.norm()` for magnitude, `.normalized()` for unit vector
    - Always check for division by zero when needed
    - Available types: `ti.f32`, `ti.f64`, `ti.i32`, `ti.i64`, `ti.u32`, `ti.u64`

6.  Output Format: Your output MUST be ONLY the Python code for the kernel. Do NOT include any markdown, explanations, or other text.

This is a test of your ability to follow instructions precisely. Any deviation will result in particles that don't move.