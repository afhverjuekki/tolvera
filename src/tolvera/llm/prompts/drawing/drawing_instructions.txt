## DRAWING KERNEL REQUIREMENTS ({interaction_type})
Generate a Taichi kernel for {description}.

{function_signature}

{draw_order_info}

IMPORTANT:
- Use @ti.kernel NOT @ti.func
- Access particles with tv.p.field[i]{additional_access}
- Use Pixels API methods:
  * tv.px.line(x1, y1, x2, y2, color) for lines/trails
  * tv.px.circle(x, y, radius, color) for circles/halos/glows
  * tv.px.rect(x, y, width, height, color) for rectangles/blocks
- NEVER use px.px.rgba[x,y] - this is incorrect!
- Colors are ti.math.vec4(r, g, b, a) with values 0.0-1.0
- Cast positions to appropriate types for drawing:
  x = ti.cast(tv.p.field[i].pos[0], ti.i32)
- Handle boundaries with modulo: x % tv.x
- {example_code}